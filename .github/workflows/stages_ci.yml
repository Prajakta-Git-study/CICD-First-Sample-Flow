name: Pipeline with Stages
on: 
  push:
    branches: [ main ]
jobs:                                      #jobs name can be anything
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Build Stage
        run: echo "Building the project..."
      
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Test Stage
        run: echo "Running tests..."

  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy Stage
        run: echo "Deploying the project..."